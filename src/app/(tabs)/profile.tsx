import { View, Text } from "react-native";

export default function ProfileScreen() {
  // This is the component that will be rendered when the user navigates to the profile screen.
  return (
    <View>
      <Text>Profile Screen</Text>
    </View>
  );
}
